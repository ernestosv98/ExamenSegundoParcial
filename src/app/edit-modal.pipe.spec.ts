import { EditModalPipe } from './edit-modal.pipe';

describe('EditModalPipe', () => {
  it('create an instance', () => {
    const pipe = new EditModalPipe();
    expect(pipe).toBeTruthy();
  });
});
